class Task {
  final String id;
  final String title;
  bool isCompleted;

  Task({required this.id, required this.title, this.isCompleted = false});
} // Criando a classe Task com os atributos id, title e isCompleted. O atributo isCompleted é inicializado como false por padrão.
